version: '3.3'

services:
   db:
     image: mysql:5.7
     ports:
       - "3306:3306"
     volumes:
       - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: Redhat2018
       MYSQL_DATABASE: academy
       MYSQL_USER: academy
       MYSQL_PASSWORD: Redhat2018
volumes:
    db_data: {}